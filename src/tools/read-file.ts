/**
 * @file src/tools/read-file.ts
 * @description Utility tool for deep code context retrieval.
 * @why Sometimes a PR diff does not contain enough context (e.g., calling a function defined elsewhere). The AI needs a way to read full files.
 * @how Provides a safe, read-only interface to read file contents from the local workspace.
 * @input A relative file path.
 * @output The raw string content of the requested file.
 */

// Implementation will go here...
